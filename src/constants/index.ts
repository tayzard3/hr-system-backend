export * from './permission'

export const NODE_ENV = Object.freeze({
	DEVELOPMENT: 'development',
	PRODUCTION: 'production',
	TEST: 'test',
})

export const USER_STATUS = Object.freeze({
	ACTIVE: 'ACTIVE',
	INACTIVE: 'INACTIVE',
})
